{% extends 'base.html.twig' %}

{% block title %}
    {{ 'comment.title_delete'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'comment.title_delete'|trans }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="form-group row float-sm_right">
        <input type="submit" value="Usun" class="btn btn-primary">

    </div>

    <div class="form-group row float-sm-left">
        <a href="{{ url('book_index') }}" class="btn btn-link">
            {{ 'action.back_to_list'|trans }}
        </a>
    </div>
    <a class="btn btn-primary"  href="{{ url('category_index') }}" role="button">
        {{ 'action_back_to_list'|trans }}
    </a>

    {{ form_end(form) }}
{% endblock %}